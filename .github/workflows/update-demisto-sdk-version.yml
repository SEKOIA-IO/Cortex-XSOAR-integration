name: update-demisto-sdk-version
on:
  workflow_dispatch:
    inputs:
      reviewer:
        description: The reviewer of the PR.
        required: true
        type: string
      release_version:
        description: The SDK release version
        required: true
        type: string
      is_draft:
        description: Is draft pull request
        required: false
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  update-dep:
    permissions:
      contents: write  # for Git to git push
    runs-on: ubuntu-latest

    steps:
    - name: Install poetry
      uses: Gr1N/setup-poetry@48b0f77c8c1b1b19cb962f0f00dff7b4be8f81ec # v9

    - name: Checkout
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        fetch-depth: 0
        token: ${{ secrets.CONTENTBOT_GH_ADMIN_TOKEN }}

    - name: Setup python
      uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
      with:
        python-version: '3.10'
        cache: 'poetry'

    - name: Create branch
      run: |
        git config --global user.email "bot@demisto.com"
        git config --global user.name "Content Bot"
        git checkout -b "${INPUTS_RELEASE_VERSION}"
      env:
        INPUTS_RELEASE_VERSION: ${{ inputs.release_version }}

    - name: Update dependencies
      continue-on-error: true
      run: |
        poetry add --group dev "demisto-sdk@${INPUTS_RELEASE_VERSION}"
        poetry lock --no-update
        git add .
        source .venv/bin/activate
        demisto-sdk pre-commit --mode=ci
        git add .
      env:
        INPUTS_RELEASE_VERSION: ${{inputs.release_version}}

    - name: Create pull request
      run: |
        git commit -m "poetry files" -a
        git push origin "${INPUTS_RELEASE_VERSION}"
        if ${{ inputs.is_draft == true }}; then
          echo "creating draft release pull request"
          gh pr create -B master -H "${INPUTS_RELEASE_VERSION}" --title "demisto-sdk-release ${INPUTS_RELEASE_VERSION}" --body "Initial body, will be replaced." --reviewer "${INPUTS_REVIEWER}" -l "docs-approved" --draft
        else
          echo "creating release pull request"
          gh pr create -B master -H "${INPUTS_RELEASE_VERSION}" --title "demisto-sdk-release ${INPUTS_RELEASE_VERSION}" --body "Initial body, will be replaced." --reviewer "${INPUTS_REVIEWER}" -l "docs-approved"
        fi
      env:
        GH_TOKEN: ${{ secrets.CONTENTBOT_GH_ADMIN_TOKEN }}
        INPUTS_RELEASE_VERSION: ${{ inputs.release_version }}
        INPUTS_REVIEWER: ${{ inputs.reviewer }}
